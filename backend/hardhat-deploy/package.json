{
  "name": "zk-kyc-contracts",
  "version": "1.0.0",
  "description": "Hardhat deployment for ZK-KYC Groth16 verifier contracts",
  "scripts": {
    "compile": "npx hardhat compile",
    "deploy:cardona": "npx hardhat run scripts/deploy.js --network polygonCardona",
    "deploy:amoy": "npx hardhat run scripts/deploy.js --network amoy",
    "test": "npx hardhat test"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^3.0.0",
    "hardhat": "^2.19.0"
  },
  "dependencies": {
    "dotenv": "^16.3.1"
  }
}
